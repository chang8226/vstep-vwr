# Muốn chạy môi trường nào, bạn chỉ cần sửa giá trị ở đây (dev, staging, hoặc pro)
TARGET_ENV=staging

# Tài khoản test dùng để auth.setup.ts đăng nhập tự động (không dùng cho môi trường dev,
# xem giải thích isDev trong playwright.config.ts)
USER_EMAIL=
USER_PASSWORD=
